# Template. Copy to `.streamlit/secrets.toml` (which is gitignored) and paste your key.
# NEVER commit the real secrets.toml. Streamlit exposes st.secrets SERVER-SIDE only —
# the browser never receives it.
OPENAI_API_KEY = "sk-REPLACE_ME"
