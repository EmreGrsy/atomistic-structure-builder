# Atomistic Structure Builder, pip requirements.
# packmol is a compiled binary and is NOT pip installable: install it from
# conda forge (see environment.yml) or build it from source and put it on PATH.

ase>=3.29
scipy>=1.17
numpy
requests
moltemplate            # provides moltemplate.sh
streamlit>=1.59
openai>=2.0

# development
pytest
